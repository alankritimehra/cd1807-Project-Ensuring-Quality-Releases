trigger:
- main

pool: myAgentPool

variables:
  azureServiceConnectionId: 'AzureServiceConnection'
  appName: 'qualityreleaseapp'

stages:
- stage: Build
  jobs:
  - job: BuildAndTest
    steps:
    - script: |
        sudo apt-get update
        sudo apt-get install -y unzip wget nodejs npm
        sudo rm -rf /usr/local/bin/terraform
        rm -rf terraform terraform.zip terraform-bin
        wget -O terraform.zip https://releases.hashicorp.com/terraform/1.2.9/terraform_1.2.9_linux_amd64.zip
        mkdir terraform-bin
        unzip -o terraform.zip -d terraform-bin
        sudo mv terraform-bin/terraform /usr/local/bin/terraform
        terraform version
      displayName: 'Install Terraform'

    - script: |
        echo "Checking repository structure..."
        pwd
        ls -la
        find . -maxdepth 4 -type d | sort

        if [ -d "terraform/environments/test" ]; then
          cd terraform/environments/test
          terraform init -reconfigure
          terraform validate
          terraform plan
        else
          echo "Terraform folder not found, skipping plan."
        fi
      displayName: 'Terraform init validate plan'
      workingDirectory: '$(System.DefaultWorkingDirectory)'

    - script: |
        sudo npm install -g newman
      displayName: 'Install Newman'

    - script: |
        newman run TestSuite.Data-Validation.json -e Test.environment.json --reporters cli,junit --reporter-junit-export TEST-DataValidation.xml
      displayName: 'Run Data Validation Tests'
      continueOnError: true
      workingDirectory: '$(System.DefaultWorkingDirectory)/automatedtesting/postman'

    - script: |
        newman run TestSuite.Regression.json -e Test.environment.json --reporters cli,junit --reporter-junit-export TEST-Regression.xml
      displayName: 'Run Regression Tests'
      continueOnError: true
      workingDirectory: '$(System.DefaultWorkingDirectory)/automatedtesting/postman'

    - task: PublishTestResults@2
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: '**/TEST-*.xml'
        searchFolder: '$(System.DefaultWorkingDirectory)/automatedtesting/postman'
        mergeTestResults: true
        testRunTitle: 'Postman Tests'

    - task: ArchiveFiles@2
      displayName: 'Archive UI Tests'
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)/automatedtesting/selenium'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-uitests.zip'

    - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-uitests.zip'
      artifact: drop-uitests

    - task: ArchiveFiles@2
      displayName: 'Archive FakeRestAPI'
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)/automatedtesting/FakeRestAPI'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-fakerestapi.zip'

    - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-fakerestapi.zip'
      artifact: drop-fakerestapi

    - task: ArchiveFiles@2
      displayName: 'Archive Performance Tests'
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)/automatedtesting/jmeter'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-perftests.zip'

    - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-perftests.zip'
      artifact: drop-perftests

- stage: Deploy
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: DeploymentEvidence
    steps:
    - download: current
      artifact: drop-fakerestapi

    - download: current
      artifact: drop-perftests

    - download: current
      artifact: drop-uitests

    - script: |
        echo "Artifacts downloaded successfully"
        ls -R $(Pipeline.Workspace)
      displayName: 'Verify Published Artifacts'

    - script: |
        sudo apt-get update
        sudo apt-get install -y unzip wget
        wget -O jmeter.tgz https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-5.2.1.tgz
        tar -xzf jmeter.tgz
        unzip -o $(Pipeline.Workspace)/drop-perftests/$(Build.BuildId)-perftests.zip -d perftests
        find perftests -name "*.jmx"
        apache-jmeter-5.2.1/bin/jmeter -n -t $(find perftests -name "*.jmx" | head -1) -j jmeter.log -f || true
        cat jmeter.log || true
      displayName: 'Run JMeter Tests'
      continueOnError: true

    - script: |
        sudo apt-get update
        sudo apt-get install -y python3-pip unzip chromium-browser
        pip3 install selenium
        mkdir -p selenium-tests
        unzip -o $(Pipeline.Workspace)/drop-uitests/$(Build.BuildId)-uitests.zip -d selenium-tests
        cd selenium-tests
        ls -la
        python3 test_saucedemo.py || python3 login.py || true
      displayName: 'Run Selenium Tests'
      continueOnError: true
